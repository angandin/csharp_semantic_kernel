{
  "schema": 1,
  "type": "completion",
  "description": "Translate the user question into a t-sql select statement, using only the tables provided.",
  "completion": {
    "max_tokens": 5000,
    "temperature": 0.0,
    "top_p": 0.0,
    "presence_penalty": 0.0,
    "frequency_penalty": 0.0
  },
  "input": {
    "parameters": [
      {
        "name": "tables",
        "description": "List of tables, with their schema, using to translate the question into a t-sql SELECT statement",
        "defaultValue": ""
      },
      {
        "name": "question",
        "description": "The user's question to be translated into t-sql SELECT statement'",
        "defaultValue": ""
      }
    ]
  }
}